components:
  parameters:
    party_id:
      name: party_id
      description: Party ID
      in: path
      required: true
      schema:
        type: string
        format: uuid
        example: faker.random.uuid