components:
  parameters:

    disclosure_id:
      name: disclosure_id
      description: Disclosure ID
      in: path
      required: true
      schema:
        type: string
        format: uuid
        example: faker.random.uuid