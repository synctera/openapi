openapi: 3.0.3
info:
  title: Synctera API
  version: 0.5.0
  description: |
    <h2>Let's build something great.</h2><p>Welcome to the official reference documentation for Synctera APIs. Our APIs are the best way to automate your company's banking needs and are designed to be easy to understand and implement.</p><p>We're continuously growing this library and what you see here is just the start, but if you need something specific or have a question, <a class='text-blue-600' href='https://synctera.com/contact' target='_blank' rel='noreferrer'>contact us</a>.</p>
servers:
  - url: https://api.synctera.com/v0
    description: Production
  - url: https://api-sandbox.synctera.com/v0
    description: Sandbox (no real world financial impact)
tags:
  - name: Accounts
    description: Request to create and manage accounts
components: #Components
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: api_key
paths:
  /accounts:
    $ref: './paths/accounts.yml'
  /accounts/{account_id}:
    $ref: './paths/accounts@{account_id}.yml'
  /accounts/{account_id}/balance:
    $ref: './paths/accounts@{account_id}@balance.yml'
  /accounts/{account_id}/transactions:
    $ref: './paths/accounts@{account_id}@transactions.yml'
  /accounts/{account_id}/addresses:
    $ref: './paths/accounts@{account_id}@addresses.yml'
  /accounts/{account_id}/addresses/{connect_id}:
    $ref: './paths/accounts@{account_id}@addresses@{connect_id}.yml'
security:
  - bearerAuth: []

