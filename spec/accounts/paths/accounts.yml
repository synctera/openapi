summary: Accounts
post:
  summary: Create an account
  operationId: createAccount
  description: Create a shadow mode or lead mode account. You need to determine the mode according to integrator configuration, and specify the required fields accordingly.
  tags:
    - Accounts
  requestBody:
    description: Account to create
    required: true
    content:
      application/json:
        schema:
          $ref: '../schemas.yml#/components/schemas/account'
  responses:
    '201':
      description: Created account
      content:
        application/json:
          schema:
            $ref: '../schemas.yml#/components/schemas/account'
    '401':
      $ref: '../../common/responses.yml#/components/responses/unauthorized'
    '422':
      $ref: '../../common/responses.yml#/components/responses/unprocessible_entity'

get:
  summary: List accounts
  description: Get paginated list of Accounts associated
  operationId: listAccounts
  parameters:
    - $ref: '../../common/parameters.yml#/components/parameters/limit'
    - $ref: '../../common/parameters.yml#/components/parameters/page_token'
    - name: customer_id
      description: Customer ID
      in: query
      required: true
      schema:
        type: string
        format: uuid
        example: faker.random.uuid
    - name: has_details
      in: query
      description: >-
        If true, the returned accounts will include relationships, aliases, balances and recent transactions.
      schema:
        type: boolean
  tags:
    - Accounts
  responses:
    '200':
      description: List of accounts
      content:
        application/json:
          schema:
            $ref: '../schemas.yml#/components/schemas/account_list'
    '400':
      $ref: '../../common/responses.yml#/components/responses/bad_request'
    '401':
      $ref: '../../common/responses.yml#/components/responses/unauthorized'
    '403':
      $ref: '../../common/responses.yml#/components/responses/forbidden'
    '404':
      $ref: '../../common/responses.yml#/components/responses/not_found'
    '500':
      $ref: '../../common/responses.yml#/components/responses/internal_server_error'
