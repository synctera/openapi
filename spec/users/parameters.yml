components:
  parameters:
    user_id:
      name: user_id
      description: User ID
      in: path
      required: true
      schema:
        type: string
        format: uuid
        example: faker.random.uuid